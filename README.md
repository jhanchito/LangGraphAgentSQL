# Guía de Ejecución

Este proyecto utiliza `uv` para la gestión de dependencias y el entorno virtual.

## Pasos para la ejecución

1. **Sincronizar dependencias:**
   ```bash
   uv sync
   ```

2. **Ejecutar la aplicación:**
   ```bash
   uv run streamlit run src/main_monolithicsql.py
   ```
